import { definePageConfig } from "@/router";

export default definePageConfig({
  meta: {
    title: "首頁",
  },
  //define your validate here
  // validate: (to) => {
  //   return true;
  // },
});
